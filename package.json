{
  "name": "deepseek-reading-companion",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "选中英文词句，让 DeepSeek 结合前后文，沿着原文直接解释。",
  "scripts": {
    "predev": "node scripts/prepare-tts.mjs",
    "prebuild": "node scripts/prepare-tts.mjs",
    "prebuild:only": "node scripts/prepare-tts.mjs",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "icons": "node scripts/make-icons.mjs",
    "preview": "npm run build && node scripts/serve-dist.mjs",
    "zip": "npm run build && node scripts/zip.mjs",
    "build:only": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@floating-ui/dom": "^1.6.13",
    "@huggingface/transformers": "3.8.1",
    "@mozilla/readability": "^0.5.0",
    "@preact/signals": "^2.0.1",
    "@types/katex": "^0.16.8",
    "dompurify": "^3.2.4",
    "katex": "^0.18.4",
    "kokoro-js": "1.2.1",
    "marked": "^15.0.7",
    "pdfjs-dist": "^6.3.289",
    "preact": "^10.26.4"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.1",
    "@preact/preset-vite": "^2.10.1",
    "@types/chrome": "^0.0.313",
    "typescript": "^5.8.2",
    "vite": "^6.2.0"
  }
}
